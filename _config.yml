title: Thinking Out Loud
domain: averagejake.com
url: https://averagejake.com
permalink: "post/:title/"

# pagination
paginate: 10
paginate_path: "page/:num/"

# custom
date_format: "%a, %b %d, %Y"

plugins: [jekyll-paginate]

# Build settings
markdown: kramdown
plugins:
  - jekyll-paginate
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
