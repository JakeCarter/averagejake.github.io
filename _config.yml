title: Thinking Out Loud
domain: jakecarter.github.io
url: https://jakecarter.github.io
permalink: "post/:title/"

# pagination
paginate: 10
paginate_path: "page/:num/"

# custom
date_format: "%a, %b %d, %Y"

plugins: [jekyll-paginate]

# Build settings
markdown: kramdown
plugins:
  - jekyll-paginate
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
